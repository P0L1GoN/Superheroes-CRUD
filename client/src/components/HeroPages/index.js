import React from 'react';
import PropTypes from 'prop-types';

const HeroPages = () => {
    return (
        <div>
            
        </div>
    );
};

HeroPages.propTypes = {};

export default HeroPages;